server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/onurokkyay/Config-Repo.git
          username: onurokkyay
          password: ${GIT_TOKEN}
  application:
    name: config-server